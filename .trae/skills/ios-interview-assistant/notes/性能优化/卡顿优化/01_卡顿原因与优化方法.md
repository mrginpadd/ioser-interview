# 卡顿原因与优化方法

## 【概念题】
**题目：** iOS App为什么会卡顿？请列举卡顿的原因和优化方法？

**答案：**

```
┌─────────────────────────────────────────────────────┐
│  卡顿原因分析                                        │
├─────────────────────────────────────────────────────┤
│  1. 主线程阻塞                                       │
│     ├── 在主线程执行网络请求                         │
│     ├── 在主线程执行数据库操作                       │
│     ├── 在主线程执行复杂计算                         │
│     └── 主线程被同步锁阻塞                           │
├─────────────────────────────────────────────────────┤
│  2. CPU计算耗时                                      │
│     ├── 布局计算复杂（AutoLayout过多）               │
│     ├── 大量视图层级                                 │
│     ├── 复杂的路径计算                               │
│     └── 重复的setNeedsLayout/setNeedsDisplay        │
├─────────────────────────────────────────────────────┤
│  3. GPU渲染耗时                                      │
│     ├── 离屏渲染（shadow、mask、圆角等）             │
│     ├── 纹理过大                                     │
│     ├── 混合操作过多                                 │
│     └── 重复渲染同一内容                             │
├─────────────────────────────────────────────────────┤
│  4. 资源问题                                         │
│     ├── 图片过大                                     │
│     ├── 内存不足导致频繁GC                           │
│     └── 磁盘I/O阻塞                                 │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  卡顿优化方法                                        │
├─────────────────────────────────────────────────────┤
│  1. 主线程优化                                       │
│     ├── 网络请求放到子线程                           │
│     ├── 数据库操作放到子线程                         │
│     ├── 复杂计算放到子线程                           │
│     └── 使用异步操作替代同步操作                     │
├─────────────────────────────────────────────────────┤
│  2. CPU优化                                         │
│     ├── 减少视图层级                                 │
│     ├── 简化AutoLayout（用Frame替代）                │
│     ├── 缓存计算结果                                 │
│     └── 避免重复调用layoutIfNeeded                  │
├─────────────────────────────────────────────────────┤
│  3. GPU优化                                         │
│     ├── 避免离屏渲染（用shadowPath替代shadow）       │
│     ├── 圆角优化（UIImageView用mask替代）            │
│     ├── 使用合适大小的图片                           │
│     └── 减少透明视图和混合操作                       │
├─────────────────────────────────────────────────────┤
│  4. TableView优化                                   │
│     ├── 使用cell复用                                 │
│     ├── 高度缓存                                     │
│     ├── 异步加载图片                                 │
│     └── 减少cell中的子视图数量                       │
├─────────────────────────────────────────────────────┤
│  5. 工具链检测                                       │
│     ├── 使用Instruments的Time Profiler检测耗时       │
│     ├── 使用Core Animation检测渲染问题               │
│     └── 使用FPS监控实时检测                          │
└─────────────────────────────────────────────────────┘
```

## 【场景题】
**题目：** 如何优化TableView的卡顿问题？

**答案：**

```objective-c
// 方案1：使用cell复用
- (UITableViewCell *)tableView:(UITableView *)tableView cellForRowAtIndexPath:(NSIndexPath *)indexPath {
    static NSString *identifier = @"Cell";
    UITableViewCell *cell = [tableView dequeueReusableCellWithIdentifier:identifier];
    if (!cell) {
        cell = [[UITableViewCell alloc] initWithStyle:UITableViewCellStyleDefault reuseIdentifier:identifier];
    }
    cell.textLabel.text = self.dataArray[indexPath.row];
    return cell;
}

// 方案2：缓存cell高度
- (CGFloat)tableView:(UITableView *)tableView heightForRowAtIndexPath:(NSIndexPath *)indexPath {
    NSNumber *height = self.heightCache[indexPath.row];
    if (height) {
        return height.floatValue;
    }
    // 计算高度
    CGFloat calculatedHeight = [self calculateHeightForIndexPath:indexPath];
    self.heightCache[indexPath.row] = @(calculatedHeight);
    return calculatedHeight;
}

// 方案3：异步加载图片
[self.imageView sd_setImageWithURL:[NSURL URLWithString:@"https://example.com/image.jpg"]
                  placeholderImage:[UIImage imageNamed:@"placeholder"]];
```

## 【代码示例】
```objective-c
// 避免离屏渲染
// ❌ 不好的做法（会触发离屏渲染）
self.view.layer.shadowColor = [UIColor blackColor].CGColor;
self.view.layer.shadowOpacity = 0.5;
self.view.layer.shadowRadius = 10;

// ✅ 好的做法（使用shadowPath避免离屏渲染）
self.view.layer.shadowColor = [UIColor blackColor].CGColor;
self.view.layer.shadowOpacity = 0.5;
self.view.layer.shadowRadius = 10;
self.view.layer.shadowPath = [[UIBezierPath bezierPathWithRect:self.view.bounds] CGPath];

// 圆角优化
// ❌ 不好的做法（会触发离屏渲染）
self.imageView.layer.cornerRadius = 20;
self.imageView.layer.masksToBounds = YES;

// ✅ 好的做法（使用mask避免离屏渲染）
UIImage *image = [UIImage imageNamed:@"avatar"];
self.imageView.image = [image imageWithRoundedCorners:CGSizeMake(20, 20)];
```